"""Módulo de calendário — feriados, dias úteis, prazos.

Sprint 2 — Ferramentas planejadas:
- listar_feriados_nacionais(ano) — feriados nacionais via BrasilAPI
- verificar_dia_util(data) — verifica se é dia útil
- calcular_prazo_util(data_inicio, dias_uteis) — soma N dias úteis
- proximo_dia_util(data) — próximo dia útil
"""

from mcp.server.fastmcp import FastMCP


def register_tools(mcp: FastMCP) -> None:
    """Registra as ferramentas de calendário no servidor MCP.

    Sprint 2 — implementação futura.
    """
    pass
